const URL_ORIGIN = 'http://localhost:5000'
const GET_SONG = `${URL_ORIGIN}/getsong/`
const GET_VIDEO = `${URL_ORIGIN}/getvideo/`
const GET_STREAMING_VIDEO = `${URL_ORIGIN}/getStreamingVideo/`
const SEARCH   = `${URL_ORIGIN}/search/`
const GET_PLAYLIST   = `${URL_ORIGIN}/getDetailPlaylist/`


export {
    URL_ORIGIN,
    GET_SONG,
    GET_VIDEO,
    GET_STREAMING_VIDEO,
    SEARCH,
    GET_PLAYLIST
}